# Copy this file to .env and fill in your values.
# Generate ADMIN_PASSWORD_HASH by running:  node -e "require('bcrypt').hash('yourpassword',12).then(console.log)"

PORT=3000
NODE_ENV=production
SESSION_SECRET=replace-with-a-long-random-string
ADMIN_PASSWORD_HASH=
